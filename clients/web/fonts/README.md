# Fonts

Uses [Ark Pixel Font](https://github.com/TakWolf/ark-pixel-font) 12px proportional
(`zh_cn` + `latin` WOFF2), OFL-1.1 — see `OFL.txt`.

Display at integer multiples of 12px (here reply uses 24px) with
`-webkit-font-smoothing: none` so glyphs stay sharp on small screens.
